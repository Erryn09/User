export default class Communication
{
    constructor(StationNum, LinkType, PortName, PortNum, Opened, Connected) {
        this.StationNum = StationNum;
        this.LinkType = LinkType;
        this.PortName = PortName;
        this.PortNum = PortNum;
        this.Opened = Opened;
        this.Connected = Connected;
    };
    // StationNum = undefined;
    // get StationNum();
    // set StationNum(value) {}
    // LinkType = undefined;
    // PortName = undefined;
    // PortNum = undefined;
    // Opened = undefined;
    // Connected = undefined;
}